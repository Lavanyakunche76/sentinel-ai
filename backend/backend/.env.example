# Sentinel AI Backend — environment variables
DATABASE_URL=sqlite:///./sentinel.db
# For production, point at a managed Postgres instance instead, e.g.:
# DATABASE_URL=postgresql://user:password@host:5432/sentinel
